#!/usr/bin/env node

/**
 * Argo CLI Entry Point
 *
 * This script serves as the executable entry point for the Argo CLI tool.
 * It loads the compiled TypeScript code and starts the CLI application.
 */

// Load the compiled CLI application
require('../dist/index.js');
